apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: monitoring
type: Opaque
data:
  # Default: admin123 — change this before deploying to production
  # To generate: echo -n 'yourpassword' | base64
  admin-password: YWRtaW4xMjM=
